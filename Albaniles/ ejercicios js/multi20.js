// Imprimir sólo los números múltiplos de 20 del 1 al 1000 por pantalla. Si x % y === 0, entonces x es múltiplo de y.

for (let i = 1; i <= 1000; i++) {
    if (i % 20 === 0) {
        alert(i);
    }
}
